
  # Patient ER Waiting App

  This is a code bundle for Patient ER Waiting App. The original project is available at https://www.figma.com/design/dFWaLfPEwEXf9zr9xbdqDV/Patient-ER-Waiting-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  